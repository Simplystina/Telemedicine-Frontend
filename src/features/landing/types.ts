export type CardProps = {
  browseDoctors: string;
  heading: string;
  text: string;
};


export type TestimonialProps = {
    testimonial: string;
    name: string;
    location: string;
    image:string
}
export interface FAQItem {
    question: string;
    answer: string;
}
